function calcular(num1, num2, operacao) {
  let resultado;

  switch (operacao) {
    case '+':
      resultado = num1 + num2;
      console.log(`Resultado: ${num1} + ${num2} = ${resultado}`);
      break;
    
  }
}